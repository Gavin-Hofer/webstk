import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import Link from "next/link";

import GithubIcon from "@/components/icons/github-icon";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Create Next App",
  description: "Generated by create next app",
};

// #region RootLayout
// =============================================================================

const RootLayout: React.FC<{
  children: React.ReactNode;
}> = ({ children }) => {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        <header className="h-10 shadow-md w-full sticky top-0 z-10">
          <nav className="flex w-full h-full gap-6 px-4 justify-between">
            <div className="flex items-center gap-4 h-full">
              <Link className="text-lg tracking-widest" href="/">
                QuickToolKit
              </Link>
            </div>
            <div className="flex items-center gap-4 h-full">
              <Link href={process.env.NEXT_PUBLIC_GITHUB_REPO_URL ?? ""}>
                <GithubIcon size={24} />
              </Link>
            </div>
          </nav>
        </header>
        <main>{children}</main>
      </body>
    </html>
  );
};

// #region Exports
// =============================================================================

export default RootLayout;
